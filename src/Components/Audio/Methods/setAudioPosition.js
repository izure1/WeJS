export default function setAudioPosition() {

  if (!this.audio) return
  if (!this.app.mainCamera) return

  const camera = this.app.mainCamera
  console.log(camera.pos)
  //this.audio.pos(cmera.)

}